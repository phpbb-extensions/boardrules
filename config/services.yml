services:
    phpbb.boardrules.listener:
        class: phpbb\boardrules\event\listener
        arguments:
            - @template
            - @controller.helper
        tags:
            - { name: event.listener }
